<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/border_white_radius4">

    <lb.themike10452.hellscorekernelmanagerl.CustomWidgets.ObservableHorizontalScrollView
        android:id="@+id/horizontalScrollView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:clipChildren="false"
        android:clipToPadding="false"
        android:paddingBottom="20dp"
        android:paddingTop="20dp"
        android:scrollbars="none">

        <LinearLayout
            android:id="@+id/itemHolder"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerVertical="true"
            android:orientation="horizontal" />
    </lb.themike10452.hellscorekernelmanagerl.CustomWidgets.ObservableHorizontalScrollView>

    <View
        android:id="@+id/leftFade"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_alignBottom="@+id/horizontalScrollView"
        android:layout_alignParentStart="true"
        android:layout_alignTop="@+id/horizontalScrollView"
        android:layout_centerVertical="true"
        android:background="@drawable/edge_fade" />

    <View
        android:id="@+id/rightFade"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_alignBottom="@+id/horizontalScrollView"
        android:layout_alignParentEnd="true"
        android:layout_alignTop="@+id/horizontalScrollView"
        android:layout_centerVertical="true"
        android:background="@drawable/edge_fade"
        android:rotation="180" />

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:src="@drawable/ic_pointer_down" />
</RelativeLayout>